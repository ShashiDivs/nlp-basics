

# Creatting Environment
1. python3 -m venv venv
# windows
2. python -m venv venv

# Activate Environment
2. source venv/bin/activate
# Windows
2. venv\Scripts\activate

# Git
1. git init
# create a repo over in your github
2. git branch -M main
3. git remote add origin <the URL of repo you cretaed>
4. git add .
5. git commit -m "" It must be meaningful
6. git push -u origin main